# ============================================================================
# AI-Powered Drug Discovery Platform - Environment Configuration
# ============================================================================
# This file documents all required environment variables for the backend.
# Copy this file to .env and update the values as needed for your environment.
#
# Requirements: 13.7 - Clear documentation for all required environment variables
# ============================================================================

# ----------------------------------------------------------------------------
# API Configuration (Requirements 13.1, 13.4)
# ----------------------------------------------------------------------------
# Port number for the FastAPI server (1-65535)
API_PORT=8000

# Host address for the API server (0.0.0.0 for all interfaces)
API_HOST=0.0.0.0

# Comma-separated list of allowed CORS origins (Requirement 13.3)
# For production, set to your frontend domain(s)
# Example: https://drugdiscovery.example.com,https://app.example.com
CORS_ORIGINS=http://localhost:3000

# ----------------------------------------------------------------------------
# External API Endpoints (Requirement 13.1)
# ----------------------------------------------------------------------------
# Open Targets Platform API - Disease-target associations
OPEN_TARGETS_API_URL=https://api.platform.opentargets.org/api/v4

# ChEMBL Database API - Bioactive molecules
CHEMBL_API_URL=https://www.ebi.ac.uk/chembl/api/data

# AlphaFold Database API - Protein structures
ALPHAFOLD_API_URL=https://alphafold.ebi.ac.uk/api

# ----------------------------------------------------------------------------
# Ollama Configuration (Requirement 13.2)
# ----------------------------------------------------------------------------
# Base URL for Ollama service (local or remote)
OLLAMA_BASE_URL=http://localhost:11434

# Model name for biomedical analysis
# Recommended: biomistral:7b (specialized for biomedical text)
OLLAMA_MODEL=biomistral:7b

# Timeout for Ollama requests in seconds (1-60)
OLLAMA_TIMEOUT=5

# ----------------------------------------------------------------------------
# Cache Configuration (Requirement 13.1)
# ----------------------------------------------------------------------------
# Redis connection URL
# Format: redis://[username:password@]host:port[/database]
# Example: redis://localhost:6379/0
REDIS_URL=redis://localhost:6379

# Cache time-to-live in seconds (minimum 60)
# Default: 86400 (24 hours)
CACHE_TTL=86400

# ----------------------------------------------------------------------------
# Rate Limiting (Requirement 13.1)
# ----------------------------------------------------------------------------
# Maximum requests per minute per IP address (minimum 1)
RATE_LIMIT_PER_MINUTE=100

# ----------------------------------------------------------------------------
# Logging Configuration (Requirement 13.5)
# ----------------------------------------------------------------------------
# Logging level: DEBUG, INFO, WARNING, ERROR, or CRITICAL
LOG_LEVEL=INFO

# ----------------------------------------------------------------------------
# Security Configuration (Requirements 12.1, 12.8)
# ----------------------------------------------------------------------------
# Enforce HTTPS for all requests in production
# Set to true in production, false in development
ENFORCE_HTTPS=false

# Environment: development, staging, or production
ENVIRONMENT=development

# ============================================================================
# Notes:
# ============================================================================
# - All URLs must start with http://, https://, or redis://
# - Port numbers must be between 1 and 65535
# - CORS origins cannot be empty
# - Cache TTL must be at least 60 seconds
# - Rate limit must be at least 1 request per minute
# - HTTPS enforcement should be enabled in production
# - IP addresses are automatically anonymized in logs (Requirement 12.8)
# - Invalid configuration will cause the application to fail on startup
#   with clear error messages (Requirement 13.8)
# ============================================================================

